rules_version = '2';
// 開発用: 本番前に必ず見直すこと。Firebase Console の Firestore → ルール に貼り付け可能。
// 詳細: docs/FIRESTORE_RULES.md
service cloud.firestore {
  match /databases/{database}/documents {
    match /leaderboard/{docId} {
      allow read, write: if true;
    }
  }
}
